"""多因子回测框架配置文件。

说明：
1. 时间范围不在这里手动维护，统一通过 `md.获取enddate.get_date_range` 获取。
2. 股票池先走 `.cursorrules` 约定的基础股票池入口。
3. 本轮按你的要求，仅额外过滤 ST，暂不加入其他交易过滤。
"""

# ==================== 日期配置 ====================
# 这里只保留占位说明，真正运行时仍以统一日期函数返回结果为准。
DATE_MODE = "auto"
START_DATE = "20241101"
END_DATE = None

# ==================== 基础股票池配置 ====================
# 这两个参数会传给 `get_universe_with_basics`。
# 含义与 `.cursorrules` 保持一致：
# - MAX_PRICE: 最新价上限
# - MAX_MCAP: 总市值上限（亿元）
MAX_PRICE = 18.0
MAX_MCAP = 200.0

# 是否在基础股票池之上进一步过滤 ST。
# 你当前要求“过滤 ST，其他不过滤”，因此这里默认开启。
ENABLE_ST_FILTER = True

# 调试参数：
# 为了避免全市场首次拉取日线过慢，可临时限制股票池数量。
# 0 表示不限制，按完整股票池运行。
DEBUG_MAX_CODES = 20

# ==================== 行情配置 ====================
# 默认使用前复权数据。
DIVIDEND_TYPE = "front"

# 0 表示优先读取缓存；1 表示强制重新下载。
NEED_DOWNLOAD = 0

# ==================== 因子参数 ====================
# 动量窗口：用于计算 20 日动量。
MOMENTUM_WINDOW = 20

# 风险调整动量窗口：用于计算 20 日收益 / 20 日波动率。
RISK_ADJUSTED_WINDOW = 20

# 两个技术因子先采用等权。
FACTOR_WEIGHTS = {
    "momentum_20": 0.5,
    "risk_adjusted_momentum_20": 0.5,
}

# ==================== 因子评估参数 ====================
# IR 年化时默认按周频近似折算。
IC_PERIODS_PER_YEAR = 52

# ==================== 因子筛选参数 ====================
# 单因子指标初筛阈值：
# - IC均值 / RankIC均值 / RR均值默认不设硬阈值；
# - ICIR / RankICIR 默认要求非负；
# - RR胜率默认至少 50%。
MIN_IC_MEAN = None
MIN_ICIR = 0.0
MIN_RANK_IC_MEAN = None
MIN_RANK_ICIR = 0.0
MIN_RR_MEAN = None
MIN_RR_WIN_RATE = 0.5

# 因子相关性去冗余阈值。
# 若两个因子横截面分数相关系数绝对值大于该阈值，则淘汰综合表现较差者。
FACTOR_CORR_THRESHOLD = 0.8

# ==================== 基准配置 ====================
# 回测基准改为中证 2000。
# 这里优先使用中证 2000 指数常用代码 `932000.CSI`。
BENCHMARK_CODE = "932000.CSI"
BENCHMARK_NAME = "中证2000"

# ==================== 组合参数 ====================
# 每周调仓一次。
REBALANCE_FREQ = "W-FRI"

# 每期持有股票数量。
HOLD_NUM = 20

# 交易费用参数。
# 按你的最新要求，手续费暂时设为 0。
COMMISSION = 0.0
SLIPPAGE = 0.0

# 初始资金。
INITIAL_CASH = 1_000_000

# ==================== 输出参数 ====================
# 结果输出目录（相对于当前模块目录）。
OUTPUT_DIR = "outputs"
